# This workflow will run tests using node and then publish a package to GitHub Packages when a release is created
# For more <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>GoVer - Luxurious Finance Platform</title>
  <style>
    body {
      margin: 0; 
      background: #000;
      color: #fff;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      text-align: center;
      padding: 2rem;
    }
    h1 {
      font-size: 3rem;
      color: gold;
      margin-bottom: 0.3rem;
    }
    h2 {
      color: #c9a238;
      margin-top: 2rem;
      margin-bottom: 1rem;
    }
    p {
      max-width: 600px;
      margin: 0 auto 1rem auto;
      color: #ccc;
      line-height: 1.5;
    }
    a.social {
      display: inline-block;
      margin: 0 10px;
      color: #fff;
      text-decoration: none;
      font-weight: bold;
      font-size: 1.2rem;
    }
    a.social:hover {
      color: gold;
    }
    button {
      background: gold;
      border: none;
      padding: 12px 30px;
      font-size: 1.2rem;
      font-weight: bold;
      border-radius: 25px;
      cursor: pointer;
      margin-top: 1rem;
    }
    button:hover {
      background: #b38f00;
    }
    input {
      width: 280px;
      padding: 10px;
      margin: 5px 0;
      border-radius: 6px;
      border: none;
      font-size: 1rem;
    }
    .login-box {
      margin: 2rem auto;
      max-width: 320px;
      background: #222;
      padding: 20px;
      border-radius: 12px;
    }
    footer {
      margin-top: 3rem;
      font-size: 0.8rem;
      color: #555;
    }
  </style>
</head>
<body>
  <header>
    <h1>GoVer</h1>
    <p>Luxurious Finance Platform to Guide Your Wealth Journey – Forex, Mindset, and Success.</p>
    <button>Join Now</button>
  </header>

  <section>
    <h2>About GoVer</h2>
    <p>GoVer is a vision by Forever Prince to help young people learn finance, dominate Forex, and live a luxurious lifestyle with purpose and intelligence.</p>
  </section>

  <section>
    <h2>Forex Academy</h2>
    <p>Learn Forex step-by-step from beginner to pro. Access charts, strategies, and downloadable PDF guides.</p>
    <button>Start Learning</button>
  </section>

  <section>
    <h2>Connect With Us</h2>
    <a href="#" class="social">WhatsApp</a>
    <a href="#" class="social">Instagram</a>
    <a href="#" class="social">Facebook</a>
    <a href="#" class="social">Twitter</a>
    <a href="#" class="social">TikTok</a>
  </section>

  <section class="login-box">
    <h2>Member Login</h2>
    <form>
      <input type="email" placeholder="Email" required /><br />
      <input type="password" placeholder="Password" required /><br />
      <button type="submit">Login</button>
    </form>
    <p>Don't have an account? <a href="#" style="color:gold;">Sign Up</a></p>
  </section>

  <footer>
    &copy; 2025 GoVer. Built by Forever Prince.
  </footer>
</body>
</html>
 see: https://docs.github.com/en/actions/publishing-packages/publishing-nodejs-packages

name: Node.js Package

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm test

  publish-gpr:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://npm.pkg.github.com/
      - run: npm ci
      - run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{secrets.GITHUB_TOKEN}}
